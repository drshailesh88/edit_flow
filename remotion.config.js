/**
 * Remotion Configuration
 *
 * Used by `npx remotion render` to locate the entry point
 * and configure rendering options.
 */

import { Config } from "@remotion/cli/config";

Config.setEntryPoint("./remotion/index.js");
